
server:
  shutdown: graceful
  
  
spring:
  jpa:
    show-sql: true
  docker:
    compose:
      stop:
        command: down
      skip:
        in-tests: false
      file:
        - ../docker-compose.yml

