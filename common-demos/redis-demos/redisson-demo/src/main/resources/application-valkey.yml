
spring:
  redis:
    redisson:
      file: classpath:redisson-valkey.yaml
  docker:
    compose:
      file:
        - ../valkey/docker-compose.yml
      stop:
        command: down
        timeout: 60s
      skip:
        in-tests: false
